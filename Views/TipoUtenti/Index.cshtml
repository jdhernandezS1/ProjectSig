@model IEnumerable<armadieti2.Models.TipoUtenteModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <p>@item.TipoUtente</p> |
                <a asp-action="Edit" asp-route-id="@item.TipoUtente">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TipoUtente">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TipoUtente">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
