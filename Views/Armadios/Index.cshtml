@model IEnumerable<armadieti2.Models.ArmadioModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LocationModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Numero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StatoArmadioModel)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategoriaArmadioModel)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LocationModel.Stabile)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Numero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StatoArmadioModel.StatoArmadio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CategoriaArmadioModel.CategoriaArmadio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdArmadio">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdArmadio">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdArmadio">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
