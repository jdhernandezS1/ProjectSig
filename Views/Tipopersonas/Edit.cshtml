@model armadieti2.Models.Tipopersona

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Tipopersona</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdTiPersona" />
            <div class="form-group">
                <label asp-for="Tipopersona1" class="control-label"></label>
                <input asp-for="Tipopersona1" class="form-control" />
                <span asp-validation-for="Tipopersona1" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
