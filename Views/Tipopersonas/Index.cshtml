@model IEnumerable<armadieti2.Models.Tipopersona>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipopersona1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipopersona1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTiPersona">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTiPersona">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTiPersona">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
